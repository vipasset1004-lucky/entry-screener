# ALGORITHM — 타점 스크리너

## 1. 유니버스 (`ohlcv_fetcher.py`)

- KOSPI + KOSDAQ
- 전 영업일 **거래대금 5억원 이상**
- 자동 제외: ETF, ETN, 스팩, 리츠, 선물, 인버스, 레버리지, 우선주

## 2. 데이터 수집

- `pykrx.stock.get_market_ohlcv(start, end, ticker)` — 200일 일봉
- ThreadPoolExecutor(12) 병렬, 재시도 3회
- 60일 미만 데이터는 무효 처리

## 3. 지표 계산 (`indicators.py`)

### 일목균형표 (9-26-52)
- 전환선 = (9일 최고 + 9일 최저) / 2
- 기준선 = (26일 최고 + 26일 최저) / 2
- 선행스팬1 = (전환선 + 기준선) / 2, 26일 forward shift
- 선행스팬2 = (52일 최고 + 52일 최저) / 2, 26일 forward shift
- 후행스팬 = 종가, 26일 backward shift
- 구름 = max/min(스팬1, 스팬2)

### 볼린저밴드 (20, 2σ)
- 중간선 = 20일 SMA
- 상/하단 = 중간선 ± 2 × 20일 표준편차
- %B = (종가 − 하단) / (상단 − 하단)
- 밴드폭 = (상단 − 하단) / 중간선 × 100
- 스퀴즈 = 밴드폭이 최근 20일 중 하위 20% 이하

### 엔벨로프 (20, ±5%)
- 중심 = 20일 SMA
- 상/하한 = 중심 × 1.05 / 0.95
- 위치 % = (종가 − 중심) / 중심 × 100
- 하한 반등 = 어제 < −5% AND 오늘 > −5%
- 상한 돌파 = 어제 < +5% AND 오늘 > +5%

## 4. 매수 점수 (`signals.py:buy_score`)

### A. 일목 (최대 40점)
| 조건 | 점수 |
|---|---|
| 가격 > 구름 위 | +15 |
| 전환선 > 기준선 | +10 |
| 최근 3일 내 골든크로스 | +5 (보너스) |
| 후행스팬 > 26일 전 가격 | +8 |
| 구름 두께 ≥ 5% | +7 |
| 구름 두께 ≥ 2% | +4 |

### B. 볼린저 (최대 30점)
| 조건 | 점수 |
|---|---|
| 중간선 위 ∧ %B 0.2~0.5 (눌림 매수) | +15 |
| 중간선 위 ∧ %B 0.5~0.7 | +6 |
| 스퀴즈 + 확장 (폭발 신호) | +10 |
| 스퀴즈만 (대기) | +5 |
| 밴드폭 < 10% (스퀴즈 아님) | +2 |

### C. 엔벨로프 (최대 30점)
| 조건 | 점수 |
|---|---|
| −3 ≤ 위치% ≤ 0 (저점 매수) | +15 |
| 0 < 위치% ≤ 3 (중심권) | +10 |
| 위치% < −3 (너무 빠짐) | +6 |
| 하한 반등 | +10 |
| 상한 돌파 직후 | +5 |

### 페널티
- RSI ≥ 80: −12
- RSI 75~79: −5
- 20일 수익률 ≥ 35%: −8

## 5. 매도 점수 (`signals.py:sell_score`)

| 조건 | 점수 |
|---|---|
| 구름 아래 | +15 |
| 데드크로스 | +12 |
| 후행스팬 < 26일 전 가격 | +8 |
| %B ≥ 1.0 (상단 돌파) | +15 |
| %B ≥ 0.9 | +8 |
| 엔벨로프 위치% ≥ 5% (상한 이탈) | +15 |
| 엔벨로프 위치% ≥ 3% | +8 |
| RSI ≥ 80 | +12 |
| RSI 75~79 | +6 |
| 20일 +30%+ 과속 | +10 |

### 매도 등급
- 🚨 70+: 청산 강력
- ⚠️ 55+: 부분 청산
- 🟡 40+: 주의

## 6. 매매가 (`trade_levels`)

new-high-screener의 1,125건 백테스트 시뮬레이션으로 검증된 룰:

- entry = 현재가
- stop  = entry × 0.93 (−7%)
- tp1   = entry × 1.20 (+20%) → 1/3 청산
- tp2   = entry × 1.50 (+50%) → 1/3 청산
- 잔여 1/3: 트레일링 (20일선 이탈 시)

보조 지점:
- 지지: 기준선, 구름 상단
- 저항: BB 상단, 엔벨로프 상한

## 7. 출력 필터

성능·용량 절감을 위해 다음만 저장:

- 매수 점수 ≥ 35 (`MIN_BUY_KEEP`)
- 또는 매도 점수 ≥ 55 (`MIN_SELL_KEEP`)

평균 약 100~300개 종목 → JSON ~150~300KB.

## 8. 3중 정렬 (`triple_aligned`)

다음 모두 만족 시 ✨ 보너스 표시:

- 가격 > 구름 위
- 가격 > 20일 SMA (볼린저 중간선)
- 엔벨로프 위치 −2% ~ +4%

## 9. 한계

- pykrx 데이터에 의존 → KRX 서버 장애 시 동시 실패
- 일봉만 사용 → 분봉 타점 정밀도는 한계
- 백테스트 없음 (가중치는 이론적 — 추후 검증 필요)
- 시장 레짐(BULL/BEAR) 미반영 — 시장 약세에는 추가 회피 필요
